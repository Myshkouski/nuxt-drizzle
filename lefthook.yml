colors: false
no_tty: true

output:
  - summary

pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint --no-error-on-unmatched-pattern {staged_files}
    check-format:
      run: pnpm fmt:check --no-error-on-unmatched-pattern {staged_files}
